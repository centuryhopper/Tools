[package]
name = "edit_distance"
version = "0.1.0"
edition = "2024"

[lib]
name = "edit_distance"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28.3", features = ["extension-module"] }
